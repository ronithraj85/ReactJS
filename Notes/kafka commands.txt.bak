
https://youtu.be/LqT396DOUEE?si=D0UDV5D5yTA38zlY

C:\kafka>

# Generate a random UUID for Kafka log directory initialization (used in KRaft mode)
.\bin\windows\kafka-storage.bat random-uuid --> generates an id

# Format the Kafka log directory using the generated UUID and server properties (initializes the KRaft metadata log)
.\bin\windows\kafka-storage.bat format -t <generatedId> -c .\config\kraft\server.properties  --> Formats metadata.

# Start the Kafka server in KRaft mode using the specified configuration
.\bin\windows\kafka-server-start.bat  .\config\kraft\server.properties

# Create a new Kafka topic named "test" with 1 partition and a replication factor of 1
.\bin\windows\kafka-topics.bat --create --topic telusko-topic --bootstrap-server localhost:9092 --partitions 1 --replication-factor 1   --> topic will be created.

# Start a Kafka console producer to send messages to "test"
.\bin\windows\kafka-console-producer.bat --topic telusko-topic --bootstrap-server localhost:9092

# Start a Kafka console consumer to read messages from "testc" from the beginning
.\bin\windows\kafka-console-consumer.bat --topic telusko-topic --from-beginning --bootstrap-server localhost:9092